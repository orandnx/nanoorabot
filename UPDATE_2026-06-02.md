# 2026-06-02 更新：知识库新增 MiniLM 引擎

## 背景

知识库页面（`/knowledge-base`）原有 Qwen（19000）、BGE（19001）两个引擎。
新增 MiniLM（all-MiniLM-L6-v2，384 维）为第三个可选引擎，对应独立 API 服务端口 19002。

## 改动清单

### 后端

| 文件 | 改动 |
|------|------|
| `webui/api/models.py` | `KnowledgeBaseSearchRequest.engine` 的 `Literal` 类型增加 `"minilm"` |
| `webui/api/routes/knowledge_base.py` | `_ENGINE_CONFIGS` 新增 minilm 条目；全部 `Literal` 类型增加 `"minilm"`；环境变量 `NANOBOT_VECTOR_API_BASE_MINILM` / `NANOBOT_VECTOR_DOCS_TABLE_MINILM` / `NANOBOT_VECTOR_CHUNKS_TABLE_MINILM` |

### 前端

| 文件 | 改动 |
|------|------|
| `web/src/hooks/useKnowledgeBase.ts` | `KnowledgeBaseEngine` 类型扩展为 `"qwen" \| "bge" \| "minilm"` |
| `web/src/pages/KnowledgeBase.tsx` | `ENGINE_OPTIONS` 加入 `"minilm"`；`ENGINE_TABLES` 加入 minilm 表映射；`useState` 初始化及 localStorage 持久化全部兼容 minilm |

### 国际化

| 文件 | 改动 |
|------|------|
| `web/src/i18n/locales/zh.json` | `engines` 段增加 `"minilm": "MiniLM"` |
| `web/src/i18n/locales/en.json` | 同上 |
| `web/src/i18n/locales/ja.json` | 同上 |
| `web/src/i18n/locales/ko.json` | 同上 |
| `web/src/i18n/locales/zh-TW.json` | 同上 |
| `web/src/i18n/locales/de.json` | 同上 |
| `web/src/i18n/locales/fr.json` | 同上 |

## 使用方式

重启前后端服务后，知识库页面引擎下拉菜单出现 **MiniLM** 选项。

| 操作 | MiniLM 目标 |
|------|------------|
| 上传文档 | `POST http://127.0.0.1:19002/documents/upload` |
| 向量搜索 | `POST http://127.0.0.1:19002/search` |
| 绑定文档表 | `LC_DEMO_DOCUMENTS_MINI`（384 维） |
| 绑定分片表 | `LC_DEMO_CHUNKS_MINI`（384 维） |

各引擎之间数据完全隔离（表名不同、维度不同），不会跨表读写。

## 关联项目

MiniLM API 服务的完整实现位于 `oracle26ai_vector_searchapi` 项目：
- `oracle_vector_api_mini.py` — MiniLM 独立 API 服务
- `embed_text.py` — 新增 `sentence_transformer` 后端
- `start_mini_oracle_vector_api.sh` — Linux 启动脚本
- 详情见该项目的 `UPDATE_2026-06-02.md`
